# Captionaut environment variables.
#
# Copy this file to .env to override defaults. Nothing here is required for
# local development. .env is gitignored.

# Where the backend stores uploaded videos, burned outputs, and the denoised
# audio cache. Defaults to backend/ in native dev, and /data inside the
# Docker container.
# CAPTIONAUT_DATA_DIR=/path/to/data

# Override the ffmpeg binary used for burn-in. By default the backend looks
# for `ffmpeg` on PATH.
# FFMPEG_BIN=/usr/local/bin/ffmpeg

# HuggingFace access token, used for pyannote.audio model downloads.
# Only read by Docker / hosted deployments. For native dev the token is
# entered through the Settings panel in the UI and stored in localStorage.
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
