// Mirror of backend/config.py — keep in sync.
// Frontend uses these to configure the Vite dev server + proxy target.
// Runtime API calls always use same-origin `/api` (see api.ts).
export const DEV_BACKEND_PORT = 8010
export const DEV_FRONTEND_PORT = 5200
